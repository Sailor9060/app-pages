# app-pages

Support and privacy policy pages for apps by Franz Weber, hosted via GitHub Pages.

Structure: one subfolder per app, each with `support.html` and `privacy.html`.

To add a new app, create a subfolder and link it from `index.html`.
